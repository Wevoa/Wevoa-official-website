# WevoaWeb Website

This is the official WevoaWeb website built with Wevoa itself.

Prerequisite:

```text
wevoa --version
```

Run it locally:

```text
wevoa start
```

Open:

```text
http://localhost:786
```

Build for production:

```text
wevoa build
wevoa serve
```
